# PetOrb 用户端设计核对

- 参考图：`C:\Users\LENOVO\Downloads\Codex 图像 2026年9月23日 13_51_38.png`（852 × 1846 像素，按 2× 密度对应约 426 × 923 CSS 像素）。
- 实现：`http://127.0.0.1:8081/`；视觉对照使用 `?preview=1`，另检查真实 API 等待态和送帧后的结果态。
- 浏览器截图：Codex in-app Browser 中于 426 × 923 和 360 × 800 视口截取并检查；截图由浏览器工具显示，未另存本地文件。
- 密度归一化：以参考图宽高各除以 2 与 426 × 923 CSS 像素视口比较。

## 核对结果

| 项目 | 结果 |
| --- | --- |
| 字体与层级 | PetOrb 字标、深绿标题、灰色辅助文字和百分比层级接近参考图；使用系统中文无衬线字体与 Arial 数字。 |
| 布局与间距 | 顶部、主卡片、双列置信度卡、快捷入口、免责声明和底部导航顺序与比例一致；主卡片高度经截图校正至 258 CSS 像素。 |
| 颜色 | 白底、深绿、浅绿和暖橙风险提示按参考图实现。 |
| 图像与图标 | 按用户要求保留宠物插画替换位；其余图标使用 MUI Icons。插画留白是有意差异。 |
| 内容 | 等待态不显示伪造的检测值；真实检测结果由现有 API 驱动；前端统一展示“牙龈相关区域”，保留辅助观察免责声明。 |
| 交互 | 首页、回放、提醒、我的导航与设备状态弹窗已在浏览器点击验证；预约医生明确禁用并标“敬请期待”。 |
| 响应式 | 360 CSS 像素宽无横向溢出；底部导航保持可见。 |
| 错误 | 默认地址页面和 JS/CSS 均返回 200；浏览器控制台无错误。 |

## 修正记录

1. 首次截图中主卡片比参考图高约 12 像素，调整摘要区域高度后主卡片为 258 像素。
2. 等待态原本使用完成勾号，改成空心状态点。
3. 真实 API 的 `gingi` 旧展示文案为“口腔溃疡”，前端统一显示“牙龈相关区域”。

无剩余 P0、P1 或 P2 问题。插画位待后续替换为正式资产。

final result: passed
